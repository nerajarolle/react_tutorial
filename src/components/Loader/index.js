import React from 'react';
import loadIcon from '../../assets/loader.gif';

const Loader = props => (
  <div>
    <img
    style={{width:50 }}
    src={loadIcon} alt="loaderIcon"  />
  </div>
)
export default Loader;
